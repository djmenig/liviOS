# LiviOS openSUSE (Leap 15.6, x86_64) packages
# Live ISO built with KIWI NG. These are the openSUSE RPM equivalents of the
# antiX (APT) package set, plus the live/boot stack required by KIWI.
# One package per line; '#' starts a comment.
#
# NOTE: This list mirrors editions/opensuse/appliance.kiwi. Keep them in sync.

# --- Live ISO / boot stack (KIWI required) ---
kernel-default
kernel-firmware-all
dracut
dracut-kiwi-live
grub2
grub2-x86_64-efi
grub2-i386-pc
grub2-branding-openSUSE
shim
systemd
plymouth
plymouth-theme-bgrt
plymouth-branding-openSUSE
timezone
tar
which
procps
iputils
iproute2
less
bash-completion
dhcp-client
fontconfig
fonts-config
patterns-openSUSE-base

# --- Desktop / window management ---
openbox

# --- Terminal (C64-themed URxvt) ---
rxvt-unicode

# --- X server stack (openSUSE Leap 15.6 = Xorg, first-class) ---
xinit          # provides startx
xterm-bin      # required by xinit
xorg-x11-server
xorg-x11-essentials  # xsetroot, xset, xrdb, xauth, etc.
xorg-x11-driver-video
xorg-x11-driver-input

# --- Boot splash (plymouth; fim not available in openSUSE OSS) ---
plymouth
plymouth-theme-bgrt
plymouth-plugin-script  # 'script' module required by the custom 'livios' theme
plymouth-branding-openSUSE

# --- Utilities / polish ---
unclutter
dialog
sudo            # demo user can run root commands
python311-pyxdg # openbox-session xdg-autostart backend

# --- Fonts ---
xorg-x11-fonts

# --- Sound ---
alsa-utils

# --- Curated applications ---
xgalaga-sdl    # in the openSUSE 'games' repo (added in appliance.kiwi)
gdash          # Boulder Dash clone (built from source, see packages/gdash/)
gcompris-qt    # Qt5 educational suite
libqt5-qtquickcontrols2   # QtQuick.Controls 2.x (gcompris-qt runtime dep)
libqt5-qtquickcontrols    # QtQuick.Controls 1.x + Dialogs
qqc2-desktop-style        # native desktop style for QtQuick.Controls 2
